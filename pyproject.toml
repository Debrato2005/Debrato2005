[project]
name = "debrato-profile"
version = "0.1.0"
description = "Dynamic terminal-style GitHub profile README generator"
requires-python = ">=3.13"

dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
]

[tool.uv]
package = false
