[project]
name = "debrato2005"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Debrato", email = "debrato2005@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
]

[project.scripts]
debrato2005 = "debrato2005:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
